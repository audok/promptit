{
  "name": "promptit",
  "version": "0.0.1",
  "description": "Slash-command prompt launcher for ChatGPT",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:test": "VITE_PROMPTIT_TEST_MODE=1 vite build",
    "typecheck": "tsc --noEmit",
    "test:e2e": "pnpm build:test && xvfb-run -a env TMPDIR=/tmp PLAYWRIGHT_EXTENSION_HEADED=1 playwright test",
    "test:e2e:live": "pnpm build:test && xvfb-run -a env TMPDIR=/tmp PLAYWRIGHT_EXTENSION_HEADED=1 playwright test --config playwright.live.config.ts",
    "test:e2e:headed": "pnpm build:test && xvfb-run -a env TMPDIR=/tmp PLAYWRIGHT_EXTENSION_HEADED=1 playwright test --headed",
    "test:e2e:ui": "pnpm build:test && xvfb-run -a env TMPDIR=/tmp PLAYWRIGHT_EXTENSION_HEADED=1 playwright test --ui"
  },
  "keywords": [],
  "author": "",
  "license": "GPL-3.0-only",
  "packageManager": "pnpm@10.33.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "pretendard": "^1.3.9",
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.4.0",
    "@playwright/test": "^1.58.2",
    "@tailwindcss/vite": "^4.2.2",
    "@types/chrome": "^0.1.38",
    "@types/node": "^25.5.0",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.1",
    "tailwindcss": "^4.2.2",
    "typescript": "^6.0.2",
    "vite": "^8.0.3"
  }
}
